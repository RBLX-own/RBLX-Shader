local Players = game:GetService("Players")
local player = Players.LocalPlayer
local gui = Instance.new("ScreenGui", player.PlayerGui)
gui.ResetOnSpawn = false

-- Main Background (black rectangle, centered popup with rounded corners)
local mainFrame = Instance.new("Frame")
mainFrame.Size = UDim2.new(0, 0, 0, 0) -- start small for animation
mainFrame.Position = UDim2.new(0.5, 0, 0.5, 0)
mainFrame.BackgroundColor3 = Color3.fromRGB(0, 0, 0) -- black rectangle
mainFrame.BackgroundTransparency = 1 -- start transparent for fade-in
mainFrame.BorderSizePixel = 0
mainFrame.AnchorPoint = Vector2.new(0.5, 0.5)
mainFrame.Active = true
mainFrame.Draggable = true
mainFrame.Parent = gui

-- Rounded corners for the main popup
local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(0, 12)
corner.Parent = mainFrame

-- Logo in top-left corner
local logo = Instance.new("ImageLabel")
logo.Size = UDim2.new(0, 60, 0, 60)
logo.Position = UDim2.new(0, 10, 0, 10)
logo.BackgroundTransparency = 1
logo.Image = "rbxassetid://INSERT_YOUR_IMAGE_ID_HERE"
logo.Parent = mainFrame

-- Title Text (Centered above link box)
local titleText = Instance.new("TextLabel")
titleText.Size = UDim2.new(1, 0, 0, 30)
titleText.Position = UDim2.new(0, 0, 0.2, -30) -- moved above the white box
titleText.BackgroundTransparency = 1
titleText.Text = "Click The Link to Copy"
titleText.TextColor3 = Color3.fromRGB(255, 255, 255)
titleText.Font = Enum.Font.SourceSansBold
titleText.TextSize = 20
titleText.TextXAlignment = Enum.TextXAlignment.Center
titleText.Parent = mainFrame

-- White Highlight Box
local whiteFrame = Instance.new("Frame")
whiteFrame.Size = UDim2.new(0.8, 0, 0, 40)
whiteFrame.Position = UDim2.new(0.1, 0, 0.3, 0)
whiteFrame.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
whiteFrame.BorderSizePixel = 0
whiteFrame.Parent = mainFrame

local whiteCorner = Instance.new("UICorner")
whiteCorner.CornerRadius = UDim.new(0, 8)
whiteCorner.Parent = whiteFrame

-- Clickable Link Text
local linkText = Instance.new("TextButton")
linkText.Size = UDim2.new(1, 0, 1, 0)
linkText.BackgroundTransparency = 1
linkText.Text = "t.me/RBLX_SCRIPTS_MYANMAR"
linkText.TextColor3 = Color3.fromRGB(0, 102, 255)
linkText.Font = Enum.Font.SourceSansBold
linkText.TextSize = 20
linkText.Parent = whiteFrame

-- Info Text
local infoText = Instance.new("TextLabel")
infoText.Size = UDim2.new(1, 0, 0, 40)
infoText.Position = UDim2.new(0, 0, 0.55, 0)
infoText.BackgroundTransparency = 1
infoText.Text = "More Scripts On Telegram"
infoText.TextColor3 = Color3.fromRGB(255, 255, 255)
infoText.Font = Enum.Font.SourceSans
infoText.TextSize = 18
infoText.Parent = mainFrame

-- Close Button (White with Black Text)
local closeBtn = Instance.new("TextButton")
closeBtn.Size = UDim2.new(0, 60, 0, 30)
closeBtn.Position = UDim2.new(1, -70, 1, -40)
closeBtn.BackgroundColor3 = Color3.fromRGB(255, 255, 255) -- White background
closeBtn.TextColor3 = Color3.fromRGB(0, 0, 0) -- Black text
closeBtn.Text = "OK"
closeBtn.Font = Enum.Font.SourceSansBold
closeBtn.TextSize = 16
closeBtn.Parent = mainFrame

-- Rounded corners for OK button
local okCorner = Instance.new("UICorner")
okCorner.CornerRadius = UDim.new(0, 6)
okCorner.Parent = closeBtn

-- Fixed Popup Animation (Fade-in & Scale)
task.wait(0.1)
-- First do the size animation
mainFrame:TweenSize(UDim2.new(0, 400, 0, 250), Enum.EasingDirection.Out, Enum.EasingStyle.Back, 0.6, true)
-- Then do the fade-in animation
local fadeInTween = game:GetService("TweenService"):Create(
    mainFrame,
    TweenInfo.new(0.6, Enum.EasingStyle.Quad, Enum.EasingDirection.Out),
    {BackgroundTransparency = 0}
)
fadeInTween:Play()

-- Copy link on click
linkText.MouseButton1Click:Connect(function()
    if setclipboard then
        setclipboard("https://t.me/RBLX_SCRIPTS_MYANMAR")
    elseif writeclipboard then
        writeclipboard("https://t.me/RBLX_SCRIPTS_MYANMAR")
    end
    linkText.Text = "Copied!"
    task.wait(1.5)
    linkText.Text = "t.me/RBLX_SCRIPTS_MYANMAR"
end)

-- Close button function
closeBtn.MouseButton1Click:Connect(function()
    -- Fade out animation
    local fadeOutTween = game:GetService("TweenService"):Create(
        mainFrame,
        TweenInfo.new(0.4, Enum.EasingStyle.Quad, Enum.EasingDirection.In),
        {BackgroundTransparency = 1}
    )
    fadeOutTween:Play()
    
    task.wait(0.4)
    mainFrame:TweenSize(UDim2.new(0, 0, 0, 0), Enum.EasingDirection.In, Enum.EasingStyle.Back, 0.5, true)
    task.wait(0.5)
    
    -- Load the second script interface
    createSecondInterface()
end)

-- Store script states
local scriptStates = {
    shaderEnabled = false,
    fpsBoostEnabled = false
}

-- Store created instances for cleanup
local createdInstances = {
    shader = {},
    fpsBoost = {}
}

-- Second Interface Function
function createSecondInterface()
    local secondGui = Instance.new("ScreenGui", player.PlayerGui)
    secondGui.ResetOnSpawn = false

    -- Main Background (black rectangle, centered popup with rounded corners)
    local secondFrame = Instance.new("Frame")
    secondFrame.Size = UDim2.new(0, 0, 0, 0) -- start small for animation
    secondFrame.Position = UDim2.new(0.5, 0, 0.5, 0)
    secondFrame.BackgroundColor3 = Color3.fromRGB(0, 0, 0) -- black rectangle
    secondFrame.BackgroundTransparency = 1 -- start transparent for fade-in
    secondFrame.BorderSizePixel = 0
    secondFrame.AnchorPoint = Vector2.new(0.5, 0.5)
    secondFrame.Active = true
    secondFrame.Draggable = true
    secondFrame.Parent = secondGui

    -- Rounded corners for the main popup
    local secondCorner = Instance.new("UICorner")
    secondCorner.CornerRadius = UDim.new(0, 12)
    secondCorner.Parent = secondFrame

    -- Title Text
    local secondTitle = Instance.new("TextLabel")
    secondTitle.Size = UDim2.new(1, 0, 0, 40)
    secondTitle.Position = UDim2.new(0, 0, 0.1, 0)
    secondTitle.BackgroundTransparency = 1
    secondTitle.Text = "Script Menu"
    secondTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
    secondTitle.Font = Enum.Font.SourceSansBold
    secondTitle.TextSize = 24
    secondTitle.TextXAlignment = Enum.TextXAlignment.Center
    secondTitle.Parent = secondFrame

    -- Button 1 - Shader
    local shaderBtn = Instance.new("TextButton")
    shaderBtn.Size = UDim2.new(0.8, 0, 0, 50)
    shaderBtn.Position = UDim2.new(0.1, 0, 0.3, 0)
    shaderBtn.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    shaderBtn.TextColor3 = Color3.fromRGB(0, 0, 0)
    shaderBtn.Text = "Shader: OFF"
    shaderBtn.Font = Enum.Font.SourceSansBold
    shaderBtn.TextSize = 18
    shaderBtn.Parent = secondFrame

    local shaderCorner = Instance.new("UICorner")
    shaderCorner.CornerRadius = UDim.new(0, 8)
    shaderCorner.Parent = shaderBtn

    -- Button 2 - FPS Boost
    local fpsBtn = Instance.new("TextButton")
    fpsBtn.Size = UDim2.new(0.8, 0, 0, 50)
    fpsBtn.Position = UDim2.new(0.1, 0, 0.6, 0)
    fpsBtn.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    fpsBtn.TextColor3 = Color3.fromRGB(0, 0, 0)
    fpsBtn.Text = "FPS Boost: OFF"
    fpsBtn.Font = Enum.Font.SourceSansBold
    fpsBtn.TextSize = 18
    fpsBtn.Parent = secondFrame

    local fpsCorner = Instance.new("UICorner")
    fpsCorner.CornerRadius = UDim.new(0, 8)
    fpsCorner.Parent = fpsBtn

    -- Close Button for second interface
    local secondCloseBtn = Instance.new("TextButton")
    secondCloseBtn.Size = UDim2.new(0, 60, 0, 30)
    secondCloseBtn.Position = UDim2.new(1, -70, 1, -40)
    secondCloseBtn.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    secondCloseBtn.TextColor3 = Color3.fromRGB(0, 0, 0)
    secondCloseBtn.Text = "Close"
    secondCloseBtn.Font = Enum.Font.SourceSansBold
    secondCloseBtn.TextSize = 16
    secondCloseBtn.Parent = secondFrame

    local secondOkCorner = Instance.new("UICorner")
    secondOkCorner.CornerRadius = UDim.new(0, 6)
    secondOkCorner.Parent = secondCloseBtn

    -- Fixed Popup Animation for second interface (Fade-in & Scale)
    task.wait(0.1)
    -- First do the size animation
    secondFrame:TweenSize(UDim2.new(0, 350, 0, 300), Enum.EasingDirection.Out, Enum.EasingStyle.Back, 0.6, true)
    -- Then do the fade-in animation
    local secondFadeInTween = game:GetService("TweenService"):Create(
        secondFrame,
        TweenInfo.new(0.6, Enum.EasingStyle.Quad, Enum.EasingDirection.Out),
        {BackgroundTransparency = 0}
    )
    secondFadeInTween:Play()

    -- Function to apply shader effects
    local function applyShader()
        local a = game.Lighting
        
        -- Store created instances for cleanup
        local b = Instance.new("BloomEffect", a)
        b.Enabled = true
        b.Intensity = 0.99
        b.Size = 9999 
        b.Threshold = 0
        table.insert(createdInstances.shader, b)
        
        local c = Instance.new("ColorCorrectionEffect", a)
        c.Brightness = 0.015
        c.Contrast = 0.25
        c.Enabled = true
        c.Saturation = 0.2
        c.TintColor = Color3.fromRGB(217, 145, 57)
        table.insert(createdInstances.shader, c)
        
        if getgenv().mode == "Summer" then
            c.TintColor = Color3.fromRGB(255, 220, 148)
        elseif getgenv().mode == "Autumn" then
            c.TintColor = Color3.fromRGB(217, 145, 57)
        else
            warn("No mode selected! Using default mode.")
        end
        
        local d = Instance.new("DepthOfFieldEffect", a)
        d.Enabled = true
        d.FarIntensity = 0.077
        d.FocusDistance = 21.54
        d.InFocusRadius = 20.77
        d.NearIntensity = 0.277
        table.insert(createdInstances.shader, d)
        
        local e = Instance.new("ColorCorrectionEffect", a)
        e.Brightness = 0
        e.Contrast = -0.07
        e.Saturation = 0
        e.Enabled = true
        e.TintColor = Color3.fromRGB(255, 247, 239)
        table.insert(createdInstances.shader, e)
        
        local e2 = Instance.new("ColorCorrectionEffect", a)
        e2.Brightness = 0.2
        e2.Contrast = 0.45
        e2.Saturation = -0.1
        e2.Enabled = true
        e2.TintColor = Color3.fromRGB(255, 255, 255)
        table.insert(createdInstances.shader, e2)
        
        local s = Instance.new("SunRaysEffect", a)
        s.Enabled = true
        s.Intensity = 0.01
        s.Spread = 0.146
        table.insert(createdInstances.shader, s)
        
        -- Apply lighting properties
        a.Ambient = Color3.fromRGB(33, 33, 33)
        a.Brightness = 5.69
        a.ColorShift_Bottom = Color3.fromRGB(0, 0, 0)
        a.ColorShift_Top = Color3.fromRGB(255, 247, 237)
        a.EnvironmentDiffuseScale = 0.105
        a.EnvironmentSpecularScale = 0.522
        a.GlobalShadows = true
        a.OutdoorAmbient = Color3.fromRGB(51, 54, 67)
        a.ShadowSoftness = 0.18
        a.GeographicLatitude = -15.525
        a.ExposureCompensation = 0.75
    end

    -- Function to remove shader effects
    local function removeShader()
        for _, instance in ipairs(createdInstances.shader) do
            if instance and instance.Parent then
                instance:Destroy()
            end
        end
        createdInstances.shader = {}
        
        -- Reset lighting to default
        local a = game.Lighting
        a.Ambient = Color3.fromRGB(0, 0, 0)
        a.Brightness = 1
        a.ColorShift_Bottom = Color3.fromRGB(0, 0, 0)
        a.ColorShift_Top = Color3.fromRGB(0, 0, 0)
        a.EnvironmentDiffuseScale = 1
        a.EnvironmentSpecularScale = 1
        a.GlobalShadows = true
        a.OutdoorAmbient = Color3.fromRGB(128, 128, 128)
        a.ShadowSoftness = 0
        a.GeographicLatitude = 0
        a.ExposureCompensation = 0
    end

    -- Function to apply FPS Boost
    local function applyFPSBoost()
        -- Store the FPS boost function
        local fpsBoostFunction = function()
            local v0=tonumber;local v1=string.byte;local v2=string.char;local v3=string.sub;local v4=string.gsub;local v5=string.rep;local v6=table.concat;local v7=table.insert;local v8=math.ldexp;local v9=getfenv or function() return _ENV;end ;local v10=setmetatable;local v11=pcall;local v12=select;local v13=unpack or table.unpack ;local v14=tonumber;local function v15(v16,v17,...) 
                -- FPS optimization code would go here
                -- This is the obfuscated FPS boost script
                
                -- Basic FPS optimizations
                settings().Rendering.QualityLevel = 1
                settings().Rendering.MeshPartDetailLevel = Enum.MeshPartDetailLevel.Level01
                game:GetService("Lighting").GlobalShadows = false
                game:GetService("Lighting").FantasySky.Enabled = false
                
                -- Reduce graphics quality for better performance
                for _, descendant in pairs(workspace:GetDescendants()) do
                    if descendant:IsA("Part") then
                        descendant.Material = Enum.Material.Plastic
                    elseif descendant:IsA("Decal") then
                        descendant.Transparency = 0.5
                    end
                end
            end
            
            -- Apply FPS optimizations
            v15()
        end
        
        -- Store the function reference
        createdInstances.fpsBoost.functionRef = fpsBoostFunction
        -- Apply FPS boost
        fpsBoostFunction()
    end

    -- Function to remove FPS Boost
    local function removeFPSBoost()
        -- Reset graphics settings
        settings().Rendering.QualityLevel = 8
        settings().Rendering.MeshPartDetailLevel = Enum.MeshPartDetailLevel.Level04
        game:GetService("Lighting").GlobalShadows = true
        
        -- Clear the function reference
        createdInstances.fpsBoost.functionRef = nil
    end

    -- Button 1 functionality (Shader) - Toggle
    shaderBtn.MouseButton1Click:Connect(function()
        if scriptStates.shaderEnabled then
            -- Turn off shader
            removeShader()
            scriptStates.shaderEnabled = false
            shaderBtn.Text = "Shader: OFF"
            shaderBtn.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
            shaderBtn.TextColor3 = Color3.fromRGB(0, 0, 0)
        else
            -- Turn on shader
            applyShader()
            scriptStates.shaderEnabled = true
            shaderBtn.Text = "Shader: ON"
            shaderBtn.BackgroundColor3 = Color3.fromRGB(0, 200, 0)
            shaderBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        end
    end)

    -- Button 2 functionality (FPS Boost) - Toggle
    fpsBtn.MouseButton1Click:Connect(function()
        if scriptStates.fpsBoostEnabled then
            -- Turn off FPS boost
            removeFPSBoost()
            scriptStates.fpsBoostEnabled = false
            fpsBtn.Text = "FPS Boost: OFF"
            fpsBtn.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
            fpsBtn.TextColor3 = Color3.fromRGB(0, 0, 0)
        else
            -- Turn on FPS boost
            applyFPSBoost()
            scriptStates.fpsBoostEnabled = true
            fpsBtn.Text = "FPS Boost: ON"
            fpsBtn.BackgroundColor3 = Color3.fromRGB(0, 200, 0)
            fpsBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        end
    end)

    -- Close button for second interface
    secondCloseBtn.MouseButton1Click:Connect(function()
        -- Fade out animation
        local secondFadeOutTween = game:GetService("TweenService"):Create(
            secondFrame,
            TweenInfo.new(0.4, Enum.EasingStyle.Quad, Enum.EasingDirection.In),
            {BackgroundTransparency = 1}
        )
        secondFadeOutTween:Play()
        
        task.wait(0.4)
        secondFrame:TweenSize(UDim2.new(0, 0, 0, 0), Enum.EasingDirection.In, Enum.EasingStyle.Back, 0.5, true)
        task.wait(0.5)
        secondGui:Destroy()
    end)
end